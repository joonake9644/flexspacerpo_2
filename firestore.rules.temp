rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // 임시 테스트용 규칙 - 인증된 사용자는 모든 데이터에 접근 가능
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}